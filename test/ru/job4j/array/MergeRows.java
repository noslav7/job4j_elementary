package ru.job4j.array;

import java.util.stream.Stream;

public class MergeRows {
    public static int[] merge(int[][] data) {
        Stream<Integer> concatenated = Stream.concat(

        );
            }
        }
    }
}
